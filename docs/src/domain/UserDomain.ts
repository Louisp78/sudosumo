class UserDomain {
    id: number;
    score: number;
    name: string;

    constructor(id: number, score: number, name: string) {
        this.id = id;
        this.score = score;
        this.name = name;
    }
}